import { routerRedux } from 'dva/router';
import { } from '../service';

export default {
  namespace: 'home',

  state: {
    status: undefined,
  },

  effects: {
    
  },

  reducers: {
    
  },
};